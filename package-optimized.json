{
  "name": "sistema-territorios",
  "version": "2.0.0",
  "description": "Sistema integral de gestión de territorios de predicación",
  "main": "src/index.html",
  "private": true,
  "keywords": ["territorios", "predicacion", "pwa", "firebase"],
  "author": "Sistema de Territorios",
  "license": "Private",
  "homepage": "https://conductores-9oct.web.app",
  "repository": {
    "type": "git",
    "url": "private"
  },
  "scripts": {
    "dev": "firebase serve --only hosting",
    "build": "node scripts/build.js",
    "deploy": "powershell -ExecutionPolicy Bypass -File scripts/deploy.ps1",
    "deploy:quick": "firebase deploy --only hosting",
    "serve": "firebase serve --only hosting --port 3000",
    "icons": "powershell -ExecutionPolicy Bypass -File scripts/generate-icons.ps1",
    "clean": "rimraf dist/*",
    "validate": "firebase deploy --dry-run"
  },
  "dependencies": {
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.30.0",
    "rimraf": "^6.0.1"
  },
  "peerDependencies": {
    "chart.js": "^4.4.6",
    "jspdf": "^2.5.1",
    "jspdf-autotable": "^3.8.2",
    "xlsx": "^0.18.5"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "browserslist": ["defaults", "not ie 11", "not op_mini all"],
  "pwa": {
    "name": "Sistema de Territorios",
    "shortName": "Territorios",
    "description": "Gestión integral de territorios de predicación",
    "themeColor": "#10b981",
    "backgroundColor": "#0f172a",
    "startUrl": "/",
    "display": "standalone"
  },
  "firebase": {
    "project": "conductores-9oct",
    "hosting": {
      "public": "dist",
      "rewrites": [
        {
          "source": "**",
          "destination": "/index.html"
        }
      ]
    }
  }
}
